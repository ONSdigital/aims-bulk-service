spring:
  cloud:
    gcp:
      sql:
        enabled: false
  datasource:
    url: jdbc:h2:mem:test
aims:
  epochs: 95|97|99
  current-epoch: 99
  default-limit: 1
  default-threshold: 10
  default-historical: true