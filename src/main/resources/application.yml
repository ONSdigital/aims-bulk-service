spring:
  application:
    name: aims-bulk-service
  cloud:
    gcp:
      project-id: YOUR-GCP-PROJECT-ID-HERE
      bigquery:
        dataset-name: bulk_status
      sql:
        database-name: bulk-status-db
        instance-connection-name: ons-aims-YOUR-GCP-PROJECT-ID-HERE:europe-west2:bulk-status-test
  codec:
    max-in-memory-size: 10MB
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 25MB
  thymeleaf:
    reactive:
      max-chunk-size: 8192
  sql:
    init:
      continue-on-error: true
      mode: always
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
  datasource:
    username: YOUR-DATABASE-USER
    password: YOUR-DATABASE-PASSWORD
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
    enabled-by-default: false
  endpoint:
    info:
      enabled: true
    health:
      enabled: true
    metrics:
      enabled: true
    prometheus:
      enabled: true
logging:
  level:
    root: INFO
    uk:
      gov:
        ons: INFO
    reactor:
      netty: INFO
aims:
  cloud-functions:
    create-cloud-task-function: https://europe-west2-ons-aims-analysis-prod.cloudfunctions.net/create-cloud-task-function
  epochs: 89|87|80|39
  current-epoch: 89
  # Update the records processed at every n% of completion
  report-frequency: 5
async:
  executor:
    thread:
      core_pool_size: 5
      # Configure the maximum number of threads
      max_pool_size: 5
      # Configure queue size
      queue_capacity: 999
      # Configure the maximum idle time of threads
      keep_alive_seconds: 60
      # Configure the name prefix of threads in the thread pool
      name:
        prefix: bulk-async-